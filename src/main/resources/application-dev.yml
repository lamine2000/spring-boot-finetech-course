server:
  port: 8081

# h2 database config
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: passer123
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2
      settings.web-allow-others: false